# 韧芽 · 给队友的项目简介

> 如果你还记得「只有前端插画房间、后端 WindFall 还在另一个仓库」——  
> 这份文档帮你用 **20 分钟** 追上现在的全功能版本。  
> 不用从头翻 git diff，按顺序读下去就行。

---

## 先说一句人话

**韧芽（Resilience Sprout）** 是一间 **夜里的插画书桌**：用户坐在桌前，用打字机写日记、跟小熊聊具体的事、在纸叠里翻看自己。

合并 WindFall 后端之后，我们 **没有** 做成传统 App 那种满屏 Tab；而是坚持 **「一个房间 + 几个物件各干各的活」**。  
你后面改 UI、改 API、改数据，都先问一句：**这件事属于打字机、小熊，还是纸叠？**

---

## 三分钟：从「合并前」到「现在」

| 你印象里的 heal-desk | 现在的 heal-desk |
|---|---|
| 首页夜房间 + 打字机 + 纸堆入口 | **还在**，仍是产品主舞台 |
| `/analysis` 成长分析页 | **已并入** `/history` 左栏，旧地址自动跳转 |
| 顶部「今日 / 记录 / 成长」导航 | **已删**，右上角只留头像占位（登录以后再接） |
| AI 聊天是占位页 | **真实多轮咨询**，聊完压缩成笔记进纸叠 |
| 纸叠 = 简单列表 | **左栏画像 + 右栏全部记录**，含小熊红笔记、划掉不计入总结 |
| 假纸纹 CSS | **真实纸材质 + 软木墙拼贴**（副页统一 junk journal 风） |
| AI 未配置 | 已接 **AI Ping**（OpenAI 兼容），见 `env.example` |

一句话：**后端能力都进来了，但界面仍以「房间隐喻」组织，而不是 WindFall 那套独立页面。**

---

## 产品分工（最重要，请背下来）

```
┌─────────────────────────────────────────────────────────┐
│  首页 · 夜房间（/）                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│  │ 打字机    │  │ 小熊      │  │ 纸堆 →   │               │
│  │ 手写输入  │  │ 具体咨询  │  │ /history │               │
│  └──────────┘  └──────────┘  └──────────┘               │
└─────────────────────────────────────────────────────────┘
```

### 1. 打字机 —— 只负责「写」

- 用户在首页点打字机 → 弹出 **手写编辑层**（journal editor overlay）
- 写完保存 → 一条 **`source: handwritten`** 的日记进后端
- **三问复盘**（我学到了什么 / 下次怎么办 / 我比以前强在哪）挂在 **当篇手写笔记** 下，在纸叠里补做，**不会** 单独改动画像左栏

### 2. 小熊 —— 只负责「聊一件具体的事」

- 入口：首页点小熊 → `/ai-analysis`
- **不预读** 全部日记；空输入时有浅灰提示，用户开口后消失
- 用追问帮用户说清范围：某一篇 / 某一天 / 某段时间 / 一个具体问题
- **完整对话** 只留在小熊（`/api/chats`）
- 聊完点「聊完了，帮我记下」→ AI 压缩成一篇 **`source: bear`** 的笔记进纸叠
- 红笔记可点回 `/ai-analysis?chat=…&note=…` 继续看对话

### 3. 纸叠 `/history` —— 所有记录的档案室

**左栏（每 7 篇刷新一次画像）**

- 韧性画像（GrowthProfile）
- 韧性折线（GrowthChart）—— **每篇一个点**，只在刷新时重画
- 自动刷新：可计入篇数 **增长** 且落到 **7 的倍数** 时
- 手动刷新：「立刻刷新左栏」
- 显示「还有 N 篇尚未计入」

**右栏（全部记录）**

- 手写笔记 + 小熊红笔记（牛皮纸 + 红胶带视觉）
- 搜索、情绪筛选、导出 Markdown/JSON
- 回收站（软删 30 天内可恢复）
- 红笔记可勾选 **「不计入总结」**：不真删，全文删除线，注释说明仅是不进总结/图表

**划掉的不算进 7 篇计数；手写 + 未划掉的红笔记才算。**

---

## 用户走一遍（帮你建立直觉）

1. 打开 `http://localhost:3010` → 看见夜窗、打字机、小熊、纸堆  
2. 打字机写一篇 → 保存 → 纸叠右栏出现一条横线纸笔记  
3. 展开笔记 → 可「补做三问」→ 弹出 ReflectionFlow（可让 AI 帮起头）  
4. 点小熊 → 说「想聊昨天开会那件事」→ 多轮对话 → 「聊完了，帮我记下」→ 纸叠多一条红笔记  
5. 左栏篇数到 7 的倍数 → 画像和折线自动更新（或手动点刷新）

---

## 技术地图（开发向，可当目录用）

### 页面

| 路径 | 作用 |
|---|---|
| `/` | 首页场景（未改分层 PNG，别轻易动） |
| `/history` | **纸叠**：画像 + 全部记录 |
| `/ai-analysis` | **小熊咨询** + 历史对话列表 |
| `/analysis` | 重定向 → `/history` |

### 副页共用 UI（新）

| 文件 | 作用 |
|---|---|
| `components/ui-layer/night-atmosphere.tsx` | 软木墙背景 + 散落装饰 |
| `components/ui-layer/paper-sheet.tsx` | 真纸材质容器（notebook / lined / graph / kraft） |
| `components/ui-layer/function-page-header.tsx` | 韧芽 + 返回桌面 |
| `components/ui-layer/scrap-decor.tsx` | 蝴蝶、胶带等装饰（不挡点击） |

素材在 `public/assets/papers/`、`public/assets/scrap/`、`public/assets/subpage-board.png`。

### API（`app/api/**`）

核心新增/重点：

- `GET/POST /api/analytics/insights` — 左栏 **7 篇快照**（读 / 立刻重算）
- `POST /api/ai/chat` · `POST /api/ai/chat/summary` · `POST /api/ai/reflect`
- `GET/POST/PATCH/DELETE /api/diary` — 含 `source`、`excluded_from_insights`、`chat_id`
- `GET/POST/PATCH /api/chats` — 小熊完整对话

逻辑层：

- `lib/insights.ts` · `lib/insightsRefresh.ts` — 7 篇规则与快照
- `lib/ai-upstream.ts` — 统一 AI Ping 调用
- `lib/storage.ts` — 本地 JSON 文件仓（`data/`，已 gitignore）
- `lib/types.ts` — **前后端契约**，改字段先看这里

### 数据存在哪

- 日记、聊天、用户画像、insights 快照 → `data/*.json`（本地开发）
- **部署到 Vercel 等 Serverless 会丢文件**，上线前必须换 DB/KV（见 `MERGE_NOTES.md`）

---

## 本地跑起来（给队友复制粘贴）

```bash
git clone git@github.com:Charis044/heal-desk.git
cd heal-desk
npm install
cp env.example .env.local   # 填入 AI Ping 的 AI_API_KEY
npm run dev                 # http://localhost:3010
```

AI 相关环境变量（仅服务端，勿提交）：

```env
AI_BASE_URL=https://aiping.cn/api/v1
AI_API_KEY=你的密钥
AI_MODEL=DeepSeek-V3.2
```

没配 AI 时：小熊和三问「帮我想想」会 503，**日记、纸叠列表、规则评分仍可用**。

若 dev 连不上或白屏，清缓存再起：

```bash
pkill -f "next dev.*3010"
rm -rf .next
ulimit -n 10240
npm run dev
```

---

## 合并后我们刻意没做的事（避免你重复劳动）

- ❌ 不做 WindFall 那套独立「成长页」—— 已并进纸叠左栏  
- ❌ 小熊不读全库日记做开场白  
- ❌ 红笔记「不计入总结」不是物理删除  
- ❌ `.env.local` 不进 git（每人本地自己配）  
- ❌ 首页场景大图（night-window、typewriter 等）除非产品明确要求，否则别换

---

## 还没做完 / 欢迎接手（诚实清单）

| 项 | 说明 |
|---|---|
| 登录 / 头像 | 右上角仅占位 |
| 持久化存储 | 文件仓 → 需 DB 才能上线 |
| 打字机 ↔ 后端 | 镜像日记可能重复，缺 stable `backendId` |
| 移动端 | 副页可用，首页场景仍需专项适配 |
| 音频资源 | 部分 vinyl track 404，不影响主流程 |

更细的合并史与回滚点见 **`MERGE_NOTES.md`**。

---

## 协作约定（少踩坑）

1. **改产品行为前先对表**：属于打字机 / 小熊 / 纸叠哪一个？  
2. **改 `lib/types.ts` 视为改契约**，同步 API + 前端 + 存储。  
3. **副页风格**走 `NightAtmosphere` + `PaperSheet`，不要在单页里另起一套背景。  
4. **AI 密钥**只放 `.env.local`，PR 里出现 key 一律打回。  
5. **大改 UI 前先拉最新 `main`** — 最近一批含纸材质、insights、小熊逻辑，冲突多发生在 `history/page.tsx`、`globals.css`。

---

## 读完还不确定从哪下手？

- 只想看产品：**打开本地 `/` 和 `/history`，按上面「用户走一遍」点一遍**  
- 只想改后端：从 `app/api/diary/route.ts` 和 `lib/insights.ts` 开始  
- 只想改 UI：从 `components/ui-layer/paper-sheet.tsx` 和 `app/history/page.tsx` 开始  
- 只想接 AI：看 `lib/ai-upstream.ts` 三个 `app/api/ai/*` 路由  

有问题直接在仓库 Issue 或群里 @，附上 **你改的是打字机 / 小熊 / 纸叠哪一条线** — 我们会快很多。

---

*文档随 `main` 更新；当前对应 commit 主题：纸叠副页视觉、7 篇画像快照、AI Ping 接入。*
